###########
# [required] Add commentMore actions
# n8n credentials - use the command `openssl rand -hex 32` to generate both
#   openssl is available by default on Linux/Mac
#   For Windows, you can use the 'Git Bash' terminal installed with git
#   Or run the command: python -c "import secrets; print(secrets.token_hex(32))"
############

N8N_ENCRYPTION_KEY=<replace_me_with_a_long_random_secret>
N8N_USER_MANAGEMENT_JWT_SECRET=<replace_me_with_a_long_random_secret>

TZ=America/New_York

POSTGRES_USER=root
POSTGRES_PASSWORD=<replace_me_with_a_secure_password>
POSTGRES_DB=n8n

N8N_DB_USER=n8napp
N8N_DB_PASSWORD=<replace_me_with_a_secure_password>

PGADMIN_DEFAULT_EMAIL=<replace_me_with_your_email_address>
PGADMIN_DEFAULT_PASSWORD=<replace_me_with_a_secure_password>

N8N_HOST=localhost
WEBHOOK_URL=http://localhost:5678/

WEBUI_SECRET_KEY=<replace_me_with_a_long_random_secret>

SEARXNG_HOSTNAME=localhost
# Use an absolute Windows path in your real .env, for example:
# LOCAL_AI_DATA_ROOT=D:/Local AI/local-ai-data/assistant-memory
LOCAL_AI_DATA_ROOT=./local-ai-data/assistant-memory

MEMORY_CAPTURE_TOKEN=replace_me_with_a_long_random_secret
OLLAMA_CHAT_MODEL=qwen2.5:14b-instruct-q4_K_M
OLLAMA_SUMMARY_MODEL=qwen2.5:7b-instruct-q4_K_M
WORLD_CONTEXT_MAX_ITEMS=12
WORLD_CONTEXT_LOOKBACK_DAYS=2

# AnythingLLM Desktop API settings for event-driven ingestion from n8n.
# On Docker Desktop for Windows, host.docker.internal is usually the right host target.
ANYTHINGLLM_API_BASE=http://host.docker.internal:3001
ANYTHINGLLM_API_KEY=<replace_me_with_a_API_secret_token_for_authentication>
ANYTHINGLLM_WORKSPACE_SLUG=replace_with_workspace_slug
ANYTHINGLLM_UPLOAD_FOLDER=assistant-memory
ANYTHINGLLM_SYNC_RUNTIME_CONTEXT=false